fn main() {
    let mut v = std::collections::VecDeque::with_capacity(10);
    v.push_back(2);
    println!(" vec q queue {:?}", v);
    // v.append(other);
    // v.split_off(at)
    // v.as_mut_slices()
    // v.back()
    // v.back_mut()
    // v.binary_search(x)
}
